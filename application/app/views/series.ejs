<!DOCTYPE html>
<html>
  <head>
    <title>Base de conocimiento</title>
    <% include includes/head.ejs %>
  </head>
  <body>
    <% include includes/header.ejs %>

    <div class="wrap">
      <div class="container">
        
        <!-- ===============================================================
          Mensaje si no hay resultados
        ================================================================ -->
        <h1 style="text-align: center">
          <%= series.length <= 0 ? "No tienes series creadas" : "" %>
        </h1>

        <!-- ===============================================================
          Series
        ================================================================ -->
        <div class="row">
          <div class="col-md-12">
            
            <ul >
              <% series.forEach(function(serie){ %>
                <li>
                  <a href="/serie/<%= serie._id %>"><%= serie.title %></a> 
                </li>         
              <% }); %>
            </ul>
          </div><!-- /.md -->
        </div><!-- /.row -->
    
      </div><!-- /.container -->
    </div><!-- /.wrap -->
    
    <% include includes/footer.ejs %>
  </body>
</html>